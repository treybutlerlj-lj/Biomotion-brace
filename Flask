from flask import Flask, render_template, request

app = Flask(__name__)

@app.route("/", methods=["GET", "PATCH"])
def homepage():
    # For demo: just the two diff graphs and flagging logic
    return "Dashboard coming soon"

if __name__ == "__main__":
    app.run(debug=True)
